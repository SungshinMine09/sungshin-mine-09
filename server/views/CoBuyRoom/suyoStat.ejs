<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>✨성신마인 09번지🔮</title>
    <link rel="stylesheet" href="/css/style.css"/>
    <link rel="stylesheet" href="/css/suyoStat.css"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Mochiy+Pop+One&display=swap" rel="stylesheet">
  </head>

  <body>
    
    <% Array.from(suyoStats).forEach( suyoStat => { %>
    <div class="gonggu_header">
      <div>
        <p><%= suyoStat.title %></p>
        <button class="gonggu_delete_button">공동구매방 삭제하기</button>
      </div>
    </div>
    <% }); %>

    <div class="gonggu_nav">
      <div>
        <nav>
          <ul>
            <li><button class="message_type_button" style="cursor: pointer;" onclick="location.href='/CoBuyRoom/<%= cobuyroom.id %>/newpost'">새소식</button></li>
            <li><button class="message_type_button" style="cursor: pointer;" onclick="location.href='/CoBuyRoom/<%= cobuyroom.id %>/chatting';">채팅</button></li>
            <li style="float: right;"><button class="grey_s_button" style="margin-left: 5px; cursor: pointer;" onclick="location.href='/CoBuyRoom/<%=cobuyroom.id%>/suyoStat';">수요조사 통계</button></li>
            <li style="float: right;"><button class="grey_s_button" style="margin-left: 5px; cursor: pointer;" onclick="location.href='/CoBuyForm/<%= cobuyroom.id %>/depositFormMaker';">입금폼 양식 작성</button></li>
            <li style="float: right;"><button class="grey_s_button" style="margin-left: 5px; cursor: pointer" onclick="location.href='/CoBuyForm/<%= cobuyroom.id %>/depositFormResult';">입금폼 통계</button></li>
          </ul>
        </nav>
      </div>
    </div>

    <div class="gonggu_suyo_stat">

      <% Array.from(suyoStats).forEach( suyoStat => { %>
      <div class="image_text">
        <img src="<%= `${suyoStat.real_url}` %>"/>
        <% if (suyoStat.current_demand >= suyoStat.min_demand)  { %>
          <p>🎉수요조사 최소인원 달성🎉</p>
        <% } %>
      </div>

      <div>
        <ul>
          <li><%= suyoStat.name %></li>
          <li><p>💜</p> <p style="color: rgb(142, 68, 173);"><%= suyoStat.current_demand%></p> <p style="font-size: 16px;"> / <%= suyoStat.min_demand %></p> <p>💜</p></li>
        </ul>
      </div>
      <% }); %>

    </div>  
</html>

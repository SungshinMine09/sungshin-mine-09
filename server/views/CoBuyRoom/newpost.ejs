<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>✨성신마인 09번지🔮</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/room.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Mochiy+Pop+One&display=swap" rel="stylesheet" />
  </head>

  <body>
    <div class="gonggu_header" style="cursor: pointer" onclick="location.href='/CoBuyRoom/<%= cobuyroom.id %>/detail';">
      <div>
        <p><%= cobuyroom.title %></p>
      </div>
    </div>

    <div class="gonggu_nav">
      <div>
        <nav>
          <ul>
            <li><button class="message_type_button" style="cursor: pointer" onclick="location.href='/CoBuyRoom/<%= cobuyroom.id %>/newpost';">새소식</button></li>
            <li><button class="message_type_button" style="cursor: pointer" onclick="location.href='/CoBuyRoom/<%= cobuyroom.id %>/chatting';">채팅</button></li>
            <% if(current_user_id == host_user_id) { %>
            <li style="float: right">
              <button class="grey_s_button" style="margin-left: 5px; cursor: pointer" onclick="location.href='/CoBuyRoom/<%=cobuyroom.id%>/suyoStat';">수요조사 통계</button>
            </li>
            <li style="float: right">
              <button class="grey_s_button" style="margin-left: 5px; cursor: pointer" onclick="location.href='/CoBuyForm/<%= cobuyroom.id %>/depositFormMaker';">입금폼 양식 작성</button>
            </li>
            <li style="float: right">
              <button class="grey_s_button" style="margin-left: 5px; cursor: pointer" onclick="location.href='/CoBuyForm/<%= form_id %>/depositFormResult';">입금폼 통계</button>
            </li>
            <% } %>
          </ul>
          <ul>
            <% if(current_user_id == host_user_id) { %>
            <li><button class="light_purple_s_button" style="cursor: pointer" onclick="location.href='/CoBuyRoom/<%= cobuyroom.id %>/createPost'">새소식 글쓰기</button></li>
            <% } %>
          </ul>
        </nav>
      </div>
    </div>

    <div class="post_board">
      <% if(newposts.length==0) { %>
      <div class="no_content">아직 새로운 소식이 없어요!</div>
      <% } %> <% for(var i = 0; i < newposts.length; i++) { %> <% post = newposts[i]; %>
      <div class="post">
        <div class="post_header">
          <span class="title"><%= post.title %></span>
          <span class="created_at"><%= dates[i] %></span>
        </div>
        <div class="post_body">
          <div class="post_text"><%- post.content %></div>
        </div>
      </div>
      <% }; %>
    </div>
  </body>
</html>
